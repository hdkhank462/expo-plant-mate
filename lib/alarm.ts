const createAlarm = (message: string) => {
  console.log(`Creating alarm: ${message}`);
};

const findAlarm = (message: string) => {
  console.log(`Finding alarm: ${message}`);
};

const deleteAlarm = (message: string) => {
  console.log(`Deleting alarm: ${message}`);
};

const updateAlarm = (message: string) => {
  console.log(`Updating alarm: ${message}`);
};

const enableAlarm = (message: string) => {
  console.log(`Enabling alarm: ${message}`);
};

const disableAlarm = (message: string) => {
  console.log(`Disabling alarm: ${message}`);
};

export { createAlarm, deleteAlarm, updateAlarm };
